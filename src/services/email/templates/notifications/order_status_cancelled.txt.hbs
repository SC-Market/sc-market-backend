Order Cancelled Hello
{{userDisplayName}}, Order "{{order.title}}" has been cancelled.

{{order.title}}
{{#if order.description}}{{order.description}}{{/if}}
{{#if order.cost}}Value: {{formatCurrency order.cost}} aUEC{{/if}}
Status: Cancelled View Order:
{{actionUrl}}